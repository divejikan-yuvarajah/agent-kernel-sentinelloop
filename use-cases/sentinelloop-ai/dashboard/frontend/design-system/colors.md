# Colors

Source of truth: `tokens.css` and `colors.ts`.

Neutral: `--ink`, `--panel`, `--panel-raised`, `--chalk`, `--muted`.  
Brand: `--maroon`, `--maroon-deep`.  
On brand fills: `--on-brand`.  
Signal (state only): `--signal-amber`, `--ember-orange`, `--hazard-red`, `--verified-teal`.
