"""Future durable persistence and storage client boundary.

Supabase / PostgreSQL for the five MVP tables and object storage for
evidence binaries. Not an Agent Kernel session store.

Implementation is intentionally deferred to a later build phase.
"""
