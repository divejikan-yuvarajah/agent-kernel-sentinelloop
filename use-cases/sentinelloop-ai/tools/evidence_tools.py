"""Future evidence metadata and storage operations.

Associate photos, voice metadata, documents, and text updates with an
incident. Store binaries outside session state; persist metadata on
incident_evidence.

Implementation is intentionally deferred to a later build phase.
"""
